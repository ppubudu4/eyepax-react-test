import React from 'react';
import { Row, Col, Button, Typography, Card, Table } from 'antd';
const Home = () => {
  return (
    <div style={{ marginTop: '1rem' }}>
      <Card>
        <Table />
      </Card>
    </div>
  );
};

export default Home;
